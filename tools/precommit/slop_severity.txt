# Per-rule severity overrides for the Tier-7 AI-slop sweep (check_slop.py).
#
# Without an override, each rule uses its editorial default in SEVERITY
# (check_slop.py): debug leftovers BLOCK; heuristic rules ADVISE. This repo
# promotes the objective code-in-head complexity thresholds below.
#
# Format: <rule>=off|advise|block   (one per line; `#` starts a comment)
#   off     drop the rule entirely
#   advise  print but never block (the heuristic default)
#   block   fail the commit on a new finding
#
# Rules: debug-leftover, swallowed-exception, silent-recovery, mutable-default,
#        todo-stub, comment-slop, generic-naming, function-too-long,
#        too-many-params, deep-nesting.
#
# Other possible overrides (commented out):
#   todo-stub=block
#   generic-naming=off

# Code-that-fits-in-your-head hard limits for every touched Python file.
slop/function-too-long=block
slop/too-many-params=block
slop/deep-nesting=block
